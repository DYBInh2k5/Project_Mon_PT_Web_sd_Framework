<?php

namespace App\Http\Controllers;

abstract class Controller
{
    // Lớp Controller cơ sở (Base Controller) mà tất cả các controller khác trong ứng dụng đều kế thừa.
    // Dùng để định nghĩa các thuộc tính, phương thức dùng chung (nếu có) cho các Controller con.
}
